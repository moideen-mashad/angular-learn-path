export interface DesignService {
  title: string;
  description: string;
  price: string;
  icon: string;
}

export interface ProcessStep {
  stepNumber: string;
  title: string;
  description: string;
}
